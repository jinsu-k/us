import React from 'react';

import { FooterContainer, FooterWrapper } from './FooterStyle';

const Footer = () => {
  return (
    <FooterContainer>
      <FooterWrapper>이 페이지에는 네이버에서 제공한 나눔글꼴이 적용되어 있습니다.</FooterWrapper>
    </FooterContainer>
  );
};

export default Footer;
